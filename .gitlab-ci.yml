image: node:latest

stages:
  - test

test:
  stage: test
  script:
    - npm install
    - node snowpdi_start.js
  artifacts:
    expire_in: 2 week
    when: always
    paths:
      - result.png
  tags:
    - docker